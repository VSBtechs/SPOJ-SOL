#include <iostream>
using namespace std;
int main()
{
	double i,j,gcd,q,w;
	int v,b;
	while(i!=(-1)&&j!=(-1))
	{
		
	    cin>>i;
	    cin>>j;
	    if (i==(-1)&&j==(-1))return 0;
	    v=i;
	    b=j;
	    int t;
        while ( v != 0 ) 
		    {
                t = v; v = b%v;  b = t;
            }
        gcd=b;
        q=i/gcd;
        w=j/gcd;
	
    	if(q==1000&&w==37)
        	{
		        cout<<"Y"<<"\n";
		    }
	    else
	        {
	        	cout<<"N"<<"\n";
	        
	        }
    }

	   return 0;	
}
